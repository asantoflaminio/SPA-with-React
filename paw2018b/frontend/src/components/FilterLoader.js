import React from 'react';
import ContentLoader from 'react-content-loader';

const FilterLoader = () => (
	<ContentLoader height={350} width={400}>
		<rect x='0' y='0' width='400' height='350' />
	</ContentLoader>
);

export default FilterLoader;
